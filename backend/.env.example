PORT=5000
JWT_SECRET=change-this-to-a-long-random-string
# Your deployed frontend URL (comma-separate multiple URLs if needed)
FRONTEND_URL=https://your-app.vercel.app
# On Render with a persistent disk, use /var/data/database.sqlite
DATABASE_PATH=./database.sqlite
